import React, { useState, useEffect } from "react";
import ReactDOM from "react-dom";
import styled from "styled-components";

const StyledSidebar = styled.div`

`;

export function SidebarSideBar(){
    return(
        <StyledSidebar>
            Sidebar
        </StyledSidebar>
    )
}